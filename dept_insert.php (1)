<?php
include 'connection.php'; 
if (isset($_POST['submit'])) {
    
    $dep_no   = mysqli_real_escape_string($conn, $_POST['deptno']);
    $dep_name = mysqli_real_escape_string($conn, $_POST['deptname']);
    $location = mysqli_real_escape_string($conn, $_POST['deptlocation']);
    $sql = "INSERT INTO department (dept_no, dept_name, location) 
            VALUES ('$dep_no', '$dep_name', '$location')";
    if (mysqli_query($conn, $sql)) {
        echo "New record entered successfully";
    } else {
        echo "Error: " . mysqli_error($conn); // 
    }
}
?>
